-- 20260913090349_phase_h_acquire_stripe_event_atomic.sql
-- Appliquée via MCP Supabase (version initiale).
-- Remplacée par 20260913155948_phase_h_acquire_stripe_event_v2_returning.sql
-- Contenu documentaire uniquement (déjà dans ledger Supabase).

-- RPC acquire_stripe_event v1 : INSERT + SELECT FOR UPDATE
-- (remplacée par v2 avec INSERT...RETURNING)
